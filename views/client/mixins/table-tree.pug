mixin table-tree(items)
    ul
        each item in items 
            li 
                a(href=`/products/${item.slug}`) #{item.title} 
                if item.children && item.children.length > 0
                    +table-tree(item.children)